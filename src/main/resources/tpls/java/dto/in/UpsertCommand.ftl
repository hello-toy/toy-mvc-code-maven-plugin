package ${basePackage}.${module}.dto.in;
import ${basePackage}.${module}.dto.${Model}DTO;
import lombok.Data;

/**
 * 类${Model}
 * 的实现描述：${ModelDescription}
 * 
 * @author ${author} ${createTime}
 */
@Data
public class Upsert${Model}Command  extends ${Model}DTO {
}
